"use client";

import { useEffect } from 'react';

// Stubbed out to prevent build errors. 
// Checkpoint URL feature is pending implementation.
export function useCheckpointSync() {
  useEffect(() => {
    // No-op
  }, []);
  
  return { copyShareUrl: () => {} };
}
