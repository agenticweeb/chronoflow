/**
 * TimeBudgetEngine — named surface for pace / finish-date UI.
 * Re-exports TimeBudgetCard so the architectural map matches production code.
 */

"use client";

export { TimeBudgetCard as TimeBudgetEngine } from "@/components/TimeBudgetCard";
export { TimeBudgetCard } from "@/components/TimeBudgetCard";
